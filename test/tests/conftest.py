"""Fixtures local to the tests/ suite.

Fixtures shared across the whole session (``grim_binary``, ``grim_home``,
``grim``) live in the top-level ``conftest.py``.
"""
from __future__ import annotations
